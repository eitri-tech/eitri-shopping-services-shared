import CustomerService from './../services/vtex/customer/vtexCustomerService'

describe('CalcService', () => {
	test('addition', () => {
		// const result = CalcService.addition(5, 15);
		// console.log("result", result);
		expect(2).toBe(20)
	})
})
